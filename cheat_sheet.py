#  Whole numbers = Integers = int
# Decimal numbers = float
# Variable = "this is the data/string placed inside the 'box' that is " "variable""
# input = tells the code to wait for user input before proceeding 
# a string = text
# ill add more later
# "10" is a string, it is text. int(10) is a number, an integer, it is a number.
# you have to tell python "10" is not a word, it is 10, a number, "10" + "10" is "1010"
# int(10) + int(10) is int(20)
# Python can only concatenate str (not "float") to str